#!/usr/bin/python3
from datetime import datetime

class Expense:
    def __init__(self, name="", category="", amount=0.00, current_datetime=datetime.now()):
       self.name = current_datetimename
        self.category = category
        self.amount = amount
        self.current_datetime = current_datetime

